services:
  open-uplift:
    build: .
    ports:
      - "7071:7070"
    volumes:
      - data:/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:7070/api/sync/status"]
      interval: 30s
      timeout: 5s
      retries: 3

volumes:
  data:
